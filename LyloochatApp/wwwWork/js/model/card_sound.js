function Card_Sound(id, code, filepath){
  // ========================================== VARIABLES =================================
  this.filepath = filepath;
  // ========================================== CONSTRUCTOR ===============================
  Card.apply(this, [id, code]);
  // ========================================== PRIVATE ===================================
  // ========================================== PRIVILEGED ================================

}
