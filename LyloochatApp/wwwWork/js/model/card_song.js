function Card_Song(id, code, filepath){
  // ========================================== VARIABLES =================================
  this.filepath = filepath;
  // ========================================== CONSTRUCTOR ===============================
  Card.apply(this, [id, code]);
  // ========================================== PRIVATE ===================================
  // ========================================== PRIVILEGED ================================

}
